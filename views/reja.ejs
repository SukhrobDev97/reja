<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>REJA VA MAQSADLAR</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body style="background-color: #b8bbd1;">
    <div class="container mt-5">
        <h1 class="display-4 text-center">REJA VA MAQSADLAR</h1>

        <div class="d-flex justify-content-center mt-4">
            <form id="create-form" action="/create-item" method="POST"
                class="d-flex w-100 bg-white shadow-sm p-2 rounded" style="max-width: 600px;">
                <input id="create-field" name="reja" type="text" class="form-control form-control-sm me-2"
                    placeholder="Yangi reja yozing..." autocomplete="off" autofocus>
                <button type="submit" class="btn btn-primary btn-sm">
                    Qoshish
                </button>

            </form>
        </div>


        <ul id="item-list" class="list-group pb-5" style="margin-top: 15px;">
            <% items.map(function(item){ %>
                <li class="list-group-item list-group-item-info d-flex align-items-center justify-content-between">
                    <span class="item-text">
                        <%=item.reja%>
                    </span>
                    <div>
                        <button data - id="<% item._id%>"
                            class="edit-me btn btn-secondary btn-sm mr-1">O'zgartirish</button>
                        <button data - id="<% item._id%>" class="delete-me btn btn-danger btn-sm">O'chirish</button>
                    </div>
                </li>

                <% }) %>
        </ul>

        <button id="clean-all" class="btn btn-danger" style="border-radius: 20px;">
            Delete all
        </button>


    </div>

    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script src='/browser.js'></script>
</body>


</html>